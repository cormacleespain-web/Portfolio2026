# Site password for onboarding gate (Step 1). Required for /api/unlock.
# If not set, the unlock API returns 503.
SITE_PASSWORD=your_secret_password

# OpenAI API key for the portfolio chat assistant. Required for /api/chat.
# Get one at https://platform.openai.com/api-keys
API_KEY=sk-your_openai_api_key
